# Recurring Booking Wizard & User Info - Walkthrough

## 1. User Information on Booking Card
เพิ่มการแสดงข้อมูลผู้จอง (User Information) ในการ์ดการจองบนหน้า "การจองของฉัน" (My Bookings) เพื่อความสะดวกในการตรวจสอบ
*   **ข้อมูลที่แสดง:** ชื่อ-นามสกุล, อีเมล, เบอร์โทรศัพท์ (ถ้ามี)
*   **การออกแบบ:** ใช้ Theme สีส้ม แยกส่วนชัดเจนจากข้อมูลเวลาและวัตถุประสงค์
*   **ประโยชน์:** สามารถแคปหน้าจอเพื่อส่งรายงานหรือยืนยันตัวตนได้ทันที

![User Info Card](https://placeholder-image-url.com) *(User can view in app)*

## 2. Recurring Booking Wizard (ระบบช่วยจองแบบต่อเนื่อง)
เพิ่มปุ่ม **"จองแบบต่อเนื่อง"** ในหน้าจองห้อง (Book Room Page) เพื่อเปิด Wizard สำหรับการจองหลายวันพร้อมกัน

### Step 1: Configuration (ตั้งค่ารูปแบบ)
เลือกรูปแบบการจองได้หลากหลาย:
*   **รายสัปดาห์ (Weekly):** เลือกวันในสัปดาห์ได้อิสระ (เช่น ทุกวันจันทร์ และ พุธ)
*   **รายวัน (Daily):** จองทุกวันตามช่วงวันที่กำหนด
*   ระบุช่วงเวลา (Start/End Time) และระยะเวลา (Start/End Date)

### Step 2: Preview & Conflict Check (ตรวจสอบความขัดแย้ง)
ระบบจะจำลองการจองล่วงหน้าและตรวจสอบกับตารางการจองที่มีอยู่จริง
*   ✅ **แสดงสรุป:** จำนวนทั้งหมด, จำนวนที่ว่าง, จำนวนที่ไม่ว่าง
*   📅 **ตารางรายการ:** แสดงรายการวันที่ทั้งหมด พร้อมสถานะ (ว่าง/ไม่ว่าง)
*   ⚠️ **Conflict Handling:** หากวันไหนไม่ว่าง ระบบจะแจ้งเตือนและระบุสาเหตุ (เช่น มีการจองโดย user อื่น) **โดยระบบจะสร้างการจองเฉพาะวันที่ว่างเท่านั้น**

### Step 3: Confirmation (ยืนยัน)
เมื่อตรวจสอบเรียบร้อย กด "ยืนยันการจอง" ระบบจะสร้าง Booking จริงลงในฐานข้อมูลเฉพาะวันที่สถานะ "ว่าง" เท่านั้น และรีเฟรชปฏิทินทันที

---
**Status:** ✅ Ready for Testing
**Files Modified:**
- `app/my-bookings/page.js`
- `app/d/booking/backend/app/Models/RecurringBooking.php`
- `app/d/booking/backend/app/Http/Controllers/Api/RecurringBookingController.php`
- `app/components/RecurringBookingModal.js` (New)
- `app/rooms/[id]/book/page.js`
