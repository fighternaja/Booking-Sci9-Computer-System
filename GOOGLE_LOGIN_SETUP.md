# 📘 คู่มือการตั้งค่า Google Login (ฉบับเต็ม)

> **สำหรับผู้ที่ต้องการเริ่มใช้งานเร็ว:** ดู `QUICK_START.md` แทน

## ขั้นตอนการตั้งค่า

### 1. สร้าง Google OAuth 2.0 Credentials

1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
2. สร้างโปรเจ็กต์ใหม่หรือเลือกโปรเจ็กต์ที่มีอยู่
3. เปิดใช้งาน **Google+ API** หรือ **Google Identity API**
4. ไปที่ **APIs & Services** > **Credentials**
5. คลิก **Create Credentials** > **OAuth 2.0 Client ID**
6. เลือกประเภทเป็น **Web application**
7. กรอกข้อมูล:
   - **Name**: ชื่อโปรเจ็กต์ (เช่น: Booking System)
   - **Authorized JavaScript origins** ⚠️ **จำเป็น**:
     - `http://localhost:3000`
     - `http://127.0.0.1:3000`
   - **Authorized redirect URIs** ⚠️ **สำคัญมาก! ต้องเพิ่มทุกแบบ**:
     - `http://localhost:3000`
     - `http://localhost:3000/`
     - `http://127.0.0.1:3000`
     - `http://127.0.0.1:3000/`
     
   **หมายเหตุ:** ถ้าไม่เพิ่ม redirect URIs จะเกิด error `redirect_uri_mismatch`
8. คัดลอก **Client ID** และ **Client Secret**

### 2. ตั้งค่า Backend (.env)

เพิ่มในไฟล์ `backend/.env`:

```env
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://127.0.0.1:8000/api/auth/google/callback
```

### 3. ตั้งค่า Frontend (.env.local)

สร้างไฟล์ `frontend/.env.local` และเพิ่ม:

```env
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here
```

**หมายเหตุ:** Client ID เดียวกันที่ใช้ใน backend และ frontend

### 4. รัน Migration (ถ้ายังไม่ได้รัน)

```bash
cd backend
php artisan migrate
```

### 5. เริ่มต้นใช้งาน

1. เริ่ม Laravel backend:
   ```bash
   cd backend
   php artisan serve
   ```

2. เริ่ม Next.js frontend:
   ```bash
   cd frontend
   npm run dev
   ```

3. เปิดเบราว์เซอร์ไปที่ `http://localhost:3000/login`
4. คลิกปุ่ม "เข้าสู่ระบบด้วย Google"

## การทดสอบ

1. ตรวจสอบว่า migration รันสำเร็จ:
   ```bash
   php artisan migrate:status
   ```

2. ตรวจสอบว่า route ทำงาน:
   ```bash
   php artisan route:list | grep google
   ```

3. ตรวจสอบ logs หากมีปัญหา:
   ```bash
   tail -f backend/storage/logs/laravel.log
   ```

## หมายเหตุสำคัญ

- **Client ID** ต้องเหมือนกันทั้งใน backend และ frontend
- **Authorized JavaScript origins** ต้องตรงกับ URL ที่ใช้ (localhost:3000 หรือ 127.0.0.1:3000)
- หากใช้ production ต้องเพิ่ม domain จริงใน Google Cloud Console
- Migration สำหรับ `google_id` ต้องรันก่อนใช้งาน

## Troubleshooting

### ⚠️ ปัญหา: "redirect_uri_mismatch" (พบบ่อยมาก!)
**ดูคู่มือแก้ไขแบบละเอียด:** `FIX_REDIRECT_URI_ERROR.md`

**วิธีแก้ไขเร็ว:**
1. ไปที่ Google Cloud Console > Credentials
2. แก้ไข OAuth 2.0 Client ID ของคุณ
3. เพิ่มใน **Authorized redirect URIs**:
   - `http://localhost:3000`
   - `http://localhost:3000/`
   - `http://127.0.0.1:3000`
   - `http://127.0.0.1:3000/`
4. บันทึกและรอ 5-10 วินาที
5. ล้าง cache และ cookies แล้วลองอีกครั้ง

### ปัญหา: "Token ไม่ถูกต้องหรือหมดอายุ"
- ตรวจสอบว่า Client ID ถูกต้อง
- ตรวจสอบว่า Authorized JavaScript origins ตั้งค่าถูกต้อง
- ตรวจสอบว่า Authorized redirect URIs ตั้งค่าถูกต้อง

### ปัญหา: "ไม่สามารถเชื่อมต่อกับ Google API"
- ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
- ตรวจสอบว่า Google APIs เปิดใช้งานแล้ว

### ปัญหา: Migration error
- ตรวจสอบว่า database connection ทำงานถูกต้อง
- ลองรัน `php artisan migrate:fresh` (ระวัง: จะลบข้อมูลทั้งหมด)

