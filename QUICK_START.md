# 🚀 Quick Start Guide - Google Login

## ⚡ เริ่มใช้งานใน 5 นาที

### 📋 สิ่งที่ต้องมี
- Google Account
- Node.js และ npm ติดตั้งแล้ว
- PHP และ Composer ติดตั้งแล้ว

### 🔧 ขั้นตอนการตั้งค่า

#### 1️⃣ สร้าง Google OAuth Credentials (2 นาที)

1. ไปที่: https://console.cloud.google.com/apis/credentials
2. เลือกโปรเจ็กต์หรือสร้างใหม่
3. คลิก **"+ CREATE CREDENTIALS"** > **"OAuth client ID"**
4. เลือก **"Web application"**
5. ตั้งค่า:
   - **Name**: `Booking System` (ชื่ออะไรก็ได้)
   - **Authorized JavaScript origins**: 
     ```
     http://localhost:3000
     http://127.0.0.1:3000
     ```
   - **Authorized redirect URIs** ⚠️ **สำคัญมาก!** ต้องเพิ่ม:
     ```
     http://localhost:3000
     http://localhost:3000/
     http://127.0.0.1:3000
     http://127.0.0.1:3000/
     ```
6. คลิก **"CREATE"**
7. **คัดลอก Client ID** (เก็บไว้ใช้ด้านล่าง)

#### 2️⃣ ตั้งค่า Backend (30 วินาที)

สร้างหรือแก้ไขไฟล์ `backend/.env`:

```env
GOOGLE_CLIENT_ID=วาง_client_id_ที่นี่
GOOGLE_CLIENT_SECRET=วาง_client_secret_ที่นี่
```

**หมายเหตุ:** Client Secret ไม่จำเป็นสำหรับการทำงาน แต่แนะนำให้ใส่

#### 3️⃣ ตั้งค่า Frontend (30 วินาที)

สร้างไฟล์ `frontend/.env.local`:

```env
NEXT_PUBLIC_GOOGLE_CLIENT_ID=วาง_client_id_เดียวกันที่นี่
```

#### 4️⃣ รัน Migration (30 วินาที)

```bash
cd backend
php artisan migrate
```

#### 5️⃣ เริ่มใช้งาน (1 นาที)

**Terminal 1 - Backend:**
```bash
cd backend
php artisan serve
```

**Terminal 2 - Frontend:**
```bash
cd frontend
npm run dev
```

#### 6️⃣ ทดสอบ (30 วินาที)

1. เปิดเบราว์เซอร์: `http://localhost:3000/login`
2. คลิกปุ่ม **"เข้าสู่ระบบด้วย Google"**
3. เลือก Google Account
4. ✅ สำเร็จ!

---

## 🎯 Checklist

- [ ] สร้าง Google OAuth Credentials
- [ ] ตั้งค่า `backend/.env` (GOOGLE_CLIENT_ID)
- [ ] ตั้งค่า `frontend/.env.local` (NEXT_PUBLIC_GOOGLE_CLIENT_ID)
- [ ] รัน `php artisan migrate`
- [ ] เริ่ม backend server
- [ ] เริ่ม frontend server
- [ ] ทดสอบ Google login

---

## ⚠️ หมายเหตุสำคัญ

1. **Client ID ต้องเหมือนกัน** ใน backend และ frontend
2. **Authorized JavaScript origins** ต้องตรงกับ URL ที่ใช้
3. **รีสตาร์ท Next.js** หลังเพิ่ม `.env.local` (กด Ctrl+C แล้วรัน `npm run dev` อีกครั้ง)

---

## 🐛 ปัญหาที่พบบ่อย

### ❌ Error: "redirect_uri_mismatch" ⚠️ **พบบ่อยมาก!**
**แก้ไข:** 
- ดู `FIX_REDIRECT_URI_ERROR.md` สำหรับคำแนะนำแบบละเอียด
- ตรวจสอบว่าเพิ่ม **Authorized redirect URIs** ใน Google Cloud Console:
  ```
  http://localhost:3000
  http://localhost:3000/
  http://127.0.0.1:3000
  http://127.0.0.1:3000/
  ```
- บันทึกการเปลี่ยนแปลงและรอ 5-10 วินาที
- ล้าง cache และ cookies

### ❌ Error: "Missing required parameter client_id"
**แก้ไข:** 
- ตรวจสอบว่า `frontend/.env.local` มี `NEXT_PUBLIC_GOOGLE_CLIENT_ID`
- รีสตาร์ท Next.js dev server

### ❌ Error: "Token ไม่ถูกต้อง"
**แก้ไข:**
- ตรวจสอบว่า Client ID ถูกต้อง
- ตรวจสอบ Authorized JavaScript origins ใน Google Console

### ❌ ปุ่ม Google login ไม่แสดง
**แก้ไข:**
- ตรวจสอบว่า `NEXT_PUBLIC_GOOGLE_CLIENT_ID` ถูกตั้งค่า
- ตรวจสอบ console ใน browser (F12)

---

## 📞 ต้องการความช่วยเหลือ?

ดูเอกสารเต็มใน `GOOGLE_LOGIN_SETUP.md`

