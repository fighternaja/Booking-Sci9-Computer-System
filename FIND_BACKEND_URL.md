# 🔍 วิธีหา Backend URL บน Railway

## ปัญหา: ERR_INVALID_HTTP_RESPONSE

**สาเหตุ:** คุณพยายามเข้าถึง database host (`centerbeam.proxy.rlwy.net`) โดยตรง ซึ่งเป็น MySQL server ไม่ใช่ web server

**วิธีแก้:** ต้องใช้ Backend Service URL แทน

---

## 📍 วิธีหา Backend Service URL

### วิธีที่ 1: ดูที่ Settings → Domains

1. **ไปที่ Railway** → service `Booking-Sci9-Computer...`
2. **คลิกแท็บ "Settings"** (ด้านบน)
3. **เลื่อนลงมาหา "Domains"** หรือ "Public URL"
4. **จะเห็น URL** เช่น:
   ```
   https://booking-sci9-computer-system-production.up.railway.app
   ```
5. **Copy URL นี้** ← นี่คือ Backend URL ที่ถูกต้อง!

---

### วิธีที่ 2: Generate Domain ใหม่ (ถ้ายังไม่มี)

1. **ไปที่ Settings → Domains**
2. **คลิก "Generate Domain"** หรือ "Create Domain"
3. **Railway จะสร้าง URL** ให้อัตโนมัติ
4. **Copy URL ที่ได้**

---

### วิธีที่ 3: ดูที่ Architecture View

1. **ไปที่หน้า Architecture** (คลิกชื่อ project)
2. **คลิกที่ service** `Booking-Sci9-Computer...`
3. **ดูที่มุมขวาบน** หรือ **ด้านล่างของ card**
4. **จะเห็น URL** หรือ **"Open"** button

---

## ✅ ทดสอบ Backend URL

หลังจากได้ URL แล้ว:

1. **เปิดใน browser:**
   ```
   https://xxx.up.railway.app/api/stats
   ```

2. **ผลลัพธ์ที่ควรได้:**
   ```json
   {
     "success": true,
     "data": {
       "total_rooms": 0,
       "total_users": 0
     }
   }
   ```

3. **ถ้าได้ JSON** → Backend ทำงาน ✅
4. **ถ้าไม่ได้** → ดู Logs

---

## 🔍 ถ้ายังไม่มี Domain

### วิธีสร้าง Domain:

1. **ไปที่ Settings → Domains**
2. **คลิก "+ Generate Domain"** หรือ **"+ Create Domain"**
3. **Railway จะสร้าง URL** เช่น:
   ```
   https://booking-sci9-computer-system-production.up.railway.app
   ```
4. **Copy URL นี้**

---

## ⚠️ สิ่งสำคัญ

- **Database Host** (`centerbeam.proxy.rlwy.net`) = MySQL server (ใช้สำหรับเชื่อมต่อ database เท่านั้น)
- **Backend Service URL** (`https://xxx.up.railway.app`) = Web server (ใช้เปิดใน browser และเรียก API)

---

## 📋 Checklist

- [ ] หา Backend Service URL จาก Settings → Domains
- [ ] Generate Domain (ถ้ายังไม่มี)
- [ ] ทดสอบ Backend URL (`/api/stats`)
- [ ] ได้ JSON response → สำเร็จ!

---

## 🆘 ถ้ายังหาไม่เจอ

บอกผมว่า:
1. คุณเห็นแท็บ "Settings" หรือไม่?
2. คุณเห็น "Domains" หรือไม่?
3. Service รันได้หรือยัง? (สถานะ Running?)

ผมจะช่วยหาตำแหน่งให้!

