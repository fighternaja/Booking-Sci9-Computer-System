# 🔧 แก้ไข Error: redirect_uri_mismatch

## ❌ ปัญหา
Error 400: `redirect_uri_mismatch` - การเข้าถึงถูกบล็อกเนื่องจาก redirect URI ไม่ตรงกัน

## ✅ วิธีแก้ไข

### ขั้นตอนที่ 1: เปิด Google Cloud Console
1. ไปที่: https://console.cloud.google.com/apis/credentials
2. เลือกโปรเจ็กต์ของคุณ
3. คลิกที่ OAuth 2.0 Client ID ที่สร้างไว้ (หรือสร้างใหม่)

### ขั้นตอนที่ 2: แก้ไข Authorized redirect URIs

ในส่วน **"Authorized redirect URIs"** ให้เพิ่มทุกบรรทัดนี้:

```
http://localhost:3000
http://localhost:3000/
http://127.0.0.1:3000
http://127.0.0.1:3000/
```

**หมายเหตุสำคัญ:**
- ต้องมีทั้งแบบที่มี `/` และไม่มี `/` (slash)
- ต้องมีทั้ง `localhost` และ `127.0.0.1`
- ไม่ต้องมี trailing slash ในบางกรณี แต่ควรเพิ่มทั้งสองแบบ

### ขั้นตอนที่ 3: ตรวจสอบ Authorized JavaScript origins

ในส่วน **"Authorized JavaScript origins"** ต้องมี:

```
http://localhost:3000
http://127.0.0.1:3000
```

**หมายเหตุ:** ไม่ต้องใส่ `/` (slash) ในส่วนนี้

### ขั้นตอนที่ 4: บันทึกการเปลี่ยนแปลง

1. คลิก **"SAVE"** หรือ **"UPDATE"**
2. รอสักครู่เพื่อให้ Google อัปเดตการตั้งค่า (ประมาณ 5-10 วินาที)

### ขั้นตอนที่ 5: ทดสอบอีกครั้ง

1. ปิดหน้า login (ถ้าเปิดอยู่)
2. ล้าง cache และ cookies (ถ้าจำเป็น):
   - กด `Ctrl + Shift + Delete` ใน Chrome/Edge
   - หรือเปิด Incognito/Private window
3. เปิด `http://localhost:3000/login` อีกครั้ง
4. คลิกปุ่ม "เข้าสู่ระบบด้วย Google"
5. ✅ ควรจะใช้งานได้แล้ว!

---

## 🔍 ตรวจสอบว่าแก้ไขถูกต้อง

### สิ่งที่ต้องมีใน Google Cloud Console:

✅ **Authorized JavaScript origins:**
- `http://localhost:3000`
- `http://127.0.0.1:3000`

✅ **Authorized redirect URIs:**
- `http://localhost:3000`
- `http://localhost:3000/`
- `http://127.0.0.1:3000`
- `http://127.0.0.1:3000/`

---

## ⚠️ ปัญหาที่พบบ่อย

### ปัญหา: ยังคงเกิด error หลังจากแก้ไขแล้ว

**แก้ไข:**
1. ตรวจสอบว่าบันทึกการเปลี่ยนแปลงแล้ว
2. รอสักครู่ (5-10 วินาที) เพื่อให้ Google อัปเดต
3. ล้าง cache และ cookies
4. ใช้ Incognito/Private window ทดสอบ
5. ตรวจสอบว่าใช้ URL ที่ถูกต้อง (`localhost:3000` หรือ `127.0.0.1:3000`)

### ปัญหา: ใช้ port อื่นที่ไม่ใช่ 3000

**แก้ไข:**
- ถ้าใช้ port อื่น (เช่น 3001, 3002) ต้องเพิ่มใน Google Cloud Console ด้วย
- เช่น: `http://localhost:3001`, `http://localhost:3001/`

---

## 📝 สรุป

Error `redirect_uri_mismatch` เกิดจาก redirect URI ที่ใช้จริงไม่ตรงกับที่ตั้งค่าใน Google Cloud Console

**วิธีแก้:** เพิ่ม redirect URIs ทั้งหมดใน Google Cloud Console ตามที่ระบุไว้ด้านบน

---

💡 **Tip:** ถ้ายังไม่ได้ผล ลองสร้าง OAuth Client ID ใหม่แล้วตั้งค่าตามขั้นตอนตั้งแต่ต้น

