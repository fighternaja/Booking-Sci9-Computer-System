# 🔧 แก้ Error redirect_uri_mismatch เร็วที่สุด

## ⚡ วิธีแก้ใน 2 นาที

### ขั้นตอนที่ 1: เปิด Google Cloud Console
1. ไปที่: https://console.cloud.google.com/apis/credentials
2. เลือกโปรเจ็กต์ของคุณ
3. คลิกที่ **OAuth 2.0 Client ID** ที่คุณสร้างไว้

### ขั้นตอนที่ 2: เพิ่ม Redirect URIs **ทั้งหมด** นี้:

ในส่วน **"Authorized redirect URIs"** ให้เพิ่มทุกบรรทัดนี้ **ไม่ต้องลบอะไร**:

```
http://localhost:3000
http://localhost:3000/
http://127.0.0.1:3000
http://127.0.0.1:3000/
```

**สำคัญมาก:** 
- ✅ ต้องมี **ทั้งแบบมี `/` และไม่มี `/`**
- ✅ ต้องมี **ทั้ง localhost และ 127.0.0.1**
- ✅ **ไม่ต้องลบ** URI เดิมที่อาจมีอยู่

### ขั้นตอนที่ 3: ตรวจสอบ Authorized JavaScript origins

ต้องมี **อย่างน้อย**:

```
http://localhost:3000
http://127.0.0.1:3000
```

**หมายเหตุ:** ในส่วนนี้ **ไม่ต้องใส่ `/` (slash)**

### ขั้นตอนที่ 4: บันทึกและรอ

1. คลิก **"SAVE"** หรือ **"UPDATE"**
2. **รออย่างน้อย 10-30 วินาที** เพื่อให้ Google อัปเดตการตั้งค่า
3. ลองปิดและเปิดเบราว์เซอร์ใหม่ (หรือใช้ Incognito window)

### ขั้นตอนที่ 5: ทดสอบ

1. เปิด `http://localhost:3000/login` 
2. คลิกปุ่ม "เข้าสู่ระบบด้วย Google"
3. ✅ ควรจะใช้งานได้แล้ว!

---

## ⚠️ ถ้ายังไม่ได้ผล:

### ตรวจสอบอีกครั้ง:
- [ ] มี redirect URI ทั้ง 4 บรรทัดข้างต้น
- [ ] มี JavaScript origins ทั้ง 2 บรรทัด
- [ ] บันทึกการเปลี่ยนแปลงแล้ว
- [ ] รอ 10-30 วินาทีแล้ว
- [ ] ล้าง cache/cookies หรือใช้ Incognito

### ลองวิธีอื่น:
1. **สร้าง OAuth Client ID ใหม่** และตั้งค่าตามขั้นตอนข้างต้น
2. **ตรวจสอบว่า URL ในเบราว์เซอร์ตรงกับที่ตั้งค่า** (localhost:3000 หรือ 127.0.0.1:3000)
3. **ตรวจสอบว่า Next.js กำลังรันอยู่** และใช้ port 3000

---

## 📸 ตัวอย่างการตั้งค่าใน Google Cloud Console:

### Authorized JavaScript origins:
```
http://localhost:3000
http://127.0.0.1:3000
```

### Authorized redirect URIs:
```
http://localhost:3000
http://localhost:3000/
http://127.0.0.1:3000
http://127.0.0.1:3000/
```

---

💡 **Tip:** ถ้าใช้ port อื่น (เช่น 3001) ให้เพิ่ม port นั้นด้วย เช่น:
- `http://localhost:3001`
- `http://localhost:3001/`

